#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a;
    cin>>a;
    for(int b=0;b<a;b++){
    string y;
    cin>>y;
    cout<<y;
    int u=y.size();
    while(u>0){
        cout<<"0";
        u--;
    }
    cout<<"\n";
    }
    return 0;
}